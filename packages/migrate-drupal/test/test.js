const fs = require('fs')
const assert = require('assert')
const defaultTest = require('./defaultTest')

describe('Running Tests', async () => {

  before(async () => {
  });

  /**
   * Run tests
   */
  defaultTest();

  after(async () => {
  })
})